# work.md

Paste a task under `## sticky` and say `go`. The agent moves it to `## current`, does the work, then moves it to `## done` with a retrospective.

---

## sticky

*(paste new task here, then say go)*

- **grubgauge — subtler Founder pill** — the green Founding Member pill + amber The Founder pill currently read as too prominent against rating cards / profile headers. Goal: same recognition, less visual shout. Levers to try, lightest-touch first: (a) **outline variant** (transparent bg + colored border/icon/number) replacing the solid fill on both kinds; (b) at compact size, **drop the `workspace_premium` icon** for FM and just render `#N` in a tighter pill — the number alone is the asset on rating cards; (c) if (a)/(b) aren't enough, shift FM off `bg-primary-container` (it competes with the brand green) toward a muted neutral like `bg-surface-container-high text-on-surface` with a subtle accent, while The Founder keeps its amber but in a softer `tertiary-container`/`on-tertiary-container` pairing. Touch points: `components/founder/FounderBadge.tsx` (only one). Verify by eyeing dashboard cards + profile + `/u/[username]` + UserListRow + SuggestedUsersRow — pill should still be unmistakable but no longer dominate.

---

## current

*(no active task)*

---

## done

*(completed tasks with retrospectives, most recent first)*

### grubgauge — ratings metadata for SEO (cuisine, location, price_level) — 2026-05-15
worked: shipped six derived columns on `public.ratings` (cuisine / city / neighborhood / state / postal_code / price_level) in one migration, all nullable so existing rows keep working, with a CHECK constraint on price_level (0-4) and two partial indexes matching the SEO query patterns we'll actually run — `(city, cuisine) where city is not null` for "best [cuisine] in [city]" and `(city, price_level) where city is not null` for "cheapest in [city]"; two small helpers (`lib/places/cuisine.ts` + `lib/places/address.ts`) mirror the priority-list discipline already used by `venueType.ts` (specific cuisines beat generic ones, sublocality falls through to neighborhood when Google's explicit `neighborhood` type is absent — which is most DFW addresses); `cuisine` is a single string (not text[]) — explicit v1 design choice so SEO URL slugs (`/city/carrollton/value-mexican`) stay stable as the data grows; the two Place Details call sites in `/rate` (autocomplete select + deep-link auto-select) now share one `PLACE_DETAILS_FIELDS` constant + one `spotSelectionFromPlace(place, fallbackId, prediction?)` helper, so a future field addition is one diff instead of two; address_components is free (Basic Data SKU, same bucket as name/types/formatted_address); price_level adds Atmosphere Data billing (~$5/1K), negligible at MVP scale and the source of truth for the "cheapest in [city]" query pattern; backfill is a plain `.mjs` script with inlined helper logic + a flagged DUPLICATED HELPERS block (avoided adding `tsx` as a dev dep just for a one-shot tool), idempotent (only touches `cuisine is null` rows), rate-limited (100ms/call), `--dry-run` supported; tsc + production build green
didn't: did NOT add UI exposure for the new fields anywhere (no "Mexican" pill on cards, no city tag) — out of scope; SEO content and future auto-generated public pages are the first consumers, in-app UI surfaces follow when there's a product reason; did NOT update the existing list-page selects (history / explore / `/u/[username]`) to pull the new columns — they're additive and unrendered today; did NOT run the backfill against the existing few rows (operator action: set `SUPABASE_SERVICE_ROLE_KEY` + `node scripts/backfill-ratings-metadata.mjs`, optionally with `--dry-run` first); did NOT remove `venue_type` / `venue_address` even though cuisine + city overlap them — `venue_type` is dining-format (fast-food/casual/fine/food-truck), still distinct from cuisine; `venue_address` is the human-readable display string; both still earn their place; did NOT seed a DFW neighborhood fallback table (Lower Greenville, Bishop Arts, Deep Ellum, etc. that Google's `neighborhood` type frequently misses) — left for a future pass once DFW rating density hits the threshold where SEO neighborhood pages become viable
lesson: when a third-party API hands you structured data alongside the field you came for, capture all of it at ingest — retrofitting derived fields later means re-paying the API per row OR parsing freeform text the API also gave you in structured form for free. Cost asymmetry: capturing one extra `address_components` field on the Place Details call is the same SKU billing as what you already pay; recovering it weeks later requires a full Place Details re-query per unique place. Cheap to do prospectively, expensive to do retroactively.
status: shipped (typecheck + lint baseline-equal + production build green; pending Supabase migration apply via SQL editor; backfill is optional and operator-run)

### grubgauge — Dashboard "Near You" venue carousel (V1) — 2026-05-14
worked: shipped the spec end-to-end in a single pass — `lib/places/{maps,geolocation,nearby,venueType}.ts` + `components/discovery/{NearbyVenueCard,NearbyVenuesRow}.tsx` + 4 small edits (rate-page Suspense wrapper for `useSearchParams` + `?placeId` auto-select wiring with one-shot consumed-flag, dashboard import + insert in both empty-state and main branches, copy reframe of the empty-state "No ratings yet" card to point at the new row above); state machine modelled as a discriminated union (probing / needsPermission / fetching / results / empty / error / hidden) matching the codebase's existing UI-state discipline (e.g. `ToggleLikeResult`); cost guardrails baked in from the first commit (Place Photos lazy via `loading="lazy"` on the card `<img>`, results capped to 8, sessionStorage caches at 30min for nearby results and 10min for coords keyed by 3-decimal lat/lng grid); `inferVenueType` extracted from `/rate` into shared `lib/places/venueType.ts` (single source of truth for Google→VenueType mapping, imported by both surfaces); already-rated filter uses the existing `applyRatingsOwnerScope` so the same query works for signed-in users (`user_id`) and guests (`device_id`) with no special casing; tsc + production build green across all 11 routes; lint at baseline (the two pre-existing `set-state-in-effect` errors in pages I didn't touch — same baseline carried through the prior 3 retros)
didn't: didn't touch `/rate`'s inline Maps SDK loader — added `lib/places/maps.ts` as a parallel loader for the dashboard side (both use the same `querySelector` check on the script tag, so they coexist safely); didn't refactor the dashboard's pre-existing `setLoading(true)` lint error (out of scope, called out in spec); didn't add a community-rating chip (V1 ships with Google's rating to avoid the empty-card cold-start problem; community chip arrives in V1.1 with the `ratings_summary_for_place_ids` RPC); didn't add a re-enable-location helper in `/profile` (V1.1); didn't introduce a venue detail page or any other surface changes; didn't add an integration test (the codebase has no test infra and adding one for this feature would dwarf the feature itself)
lesson: when a new feature needs a state machine more elaborate than `{loading, error, data}`, a discriminated union with a `kind` discriminator (probing/needsPermission/fetching/results/empty/error/hidden here) wins over multiple parallel boolean flags — the union narrows the rendering switch into a single render-time exhaustive case match, makes "what does the UI show right now" answerable from a single state field, and prevents the impossible-state bugs (e.g. fetching=true AND results=[…]) that emerge when permission, cache, fetch, and filter all carry their own flag; matches the codebase's existing `ToggleLikeResult` pattern in `lib/ratings/likes.ts`
status: shipped (typecheck + lint baseline-equal + production build green; visual QA on a real browser pending — needs an env with `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` set and the API key referrer-restricted in GCP console before public traffic; first user grant of geolocation permission is the only path that exercises the full prompt → fetch → results state transition end-to-end)

### grubgauge — TripAdvisor-flavoured restyle (token-layer flip) — 2026-05-13
worked: pre-flight scan confirmed zero raw colour literals across `src` (no `bg-red-500`, no `#hex`, only one `dark:` modifier in `BrandMark` and it duplicated the non-dark variant) — the whole app routes through ~15 semantic tokens (`bg-surface-container-low`, `text-on-surface-variant`, `text-primary`, `border-outline-variant`, etc.); restyle was therefore a 4-file diff: `grubgauge-palette.css` (added `--ta-*` light-palette tokens alongside retained legacy `--palette-*`), `globals.css` (remapped every `--color-*` slot in `@theme inline` + switched `color-scheme` from `dark` to `light`), `layout.tsx` (dropped the `dark` class from `<html>`), `BrandMark.tsx` (dropped the redundant `dark:text-primary` modifiers); contrast pass caught two issues during the work itself — original `primary-container = ta-green-soft` + `on-primary-container = ta-green-deep` would invert into deep-green-on-deep-green when buttons swap to `hover:bg-primary-fixed`, fixed by mapping `primary-container` to solid `ta-green` with white `on-primary-container` (same dark→light text contract the legacy palette used); and `text-tertiary` (amber) on cream was failing AA at the brand-illustration amber `#f5b041` (~2.4:1), darkened to `#b8731c` for ~5:1 against cream; container-tier ramp re-laddered (`lowest=cream, low=paper, container=mist, high/highest=mist-deep`) so card hover (`bg-surface-container-low → hover:bg-surface-container`) stays visible after flip; ta-green tuned to `#00754a` (TripAdvisor's deeper button green, AA-safe with white) rather than the brighter `#00aa6c` accent green; tsc + lint + production build all green (lint shows only the same two pre-existing `set-state-in-effect` errors in pages I didn't touch)
didn't: kept page-level information architecture (search heroes, owl-bubble rating widgets, restaurant-detail templates) untouched — that would have been an IA rewrite, not a restyle, and the work.md cadence is small surgical PRs not multi-week overhauls; didn't change typography (Work Sans 700 stays in for displays — TripAdvisor's Trip Sans Mega is heavier but a font swap is its own task); didn't introduce a dark-mode toggle (the legacy `--palette-*` tokens are kept in the palette file as a recoverable record, but the `@theme inline` block is light-only — re-binding for dark mode is a future task that has the breadcrumbs it needs)
lesson: when an entire app needs a visual overhaul and components route through semantic tokens, the win is to flip at the token layer, not edit components — but verify the *contrast contract* between paired tokens (e.g. `primary-container`/`on-primary-container` and the `:hover` swap to `primary-fixed`) survives the swap, because the original palette's "saturated bg + light text" pattern can silently invert to "light bg + dark text" on one end and "dark bg + dark text" on the other if you remap naively
status: shipped (typecheck + lint baseline-equal + build clean; visual QA still requires `npm run dev` + manual sweep)

### grubgauge — profile write split (updateProfile vs upsertProfile) — 2026-05-13
worked: `updateProfile` uses `.update().eq('id')` + `.select('id')` to detect zero rows and return `{ ok: false, error: 'profile_not_found' }`; `upsertProfile` now takes `UpsertProfilePatch` with required `username` so partial avatar-only inserts cannot violate NOT NULL; `/profile` avatar save uses `updateProfile` with a user-visible missing-profile message; onboarding resolves `trimmedName || profile?.username` for upsert and falls back to `updateProfile({ avatar_url })` when both are empty; shared `dispatchProfileUpdated()`; typecheck + build clean
didn't: onboarding still does not surface failed profile writes (Promise.all without checking results) — same as before this change; did not run an actual Supabase avatar upload in this environment
lesson: Supabase `.upsert` without a guaranteed INSERT key set is unsafe for partial patches — prefer typed UPDATE for incremental saves and reserve upsert for flows that always supply required columns; when a discriminated union mixes `{ error }` and `{ code }` branches, branch on `"code" in result` first so TypeScript narrows cleanly
status: shipped (pending manual avatar retry + push)

### grubgauge — fix /u/[username] 404 for usernames with URL-encoded characters — 2026-05-12
worked: one-line root cause once the symptom was identified — Next's useParams() returns the undecoded path segment in client components (vercel/next.js#64952), so "mark grout" arrived at the lookup as "mark%20grout" and the .eq("username", ...) missed; added a `decodeParam` helper around the useParams read with a try/catch fallback for malformed sequences; typecheck + build clean
didn't: didn't constrain username chars at the input layer (onboarding profile screen accepts any text) — defensive read-side decode covers all existing rows without a schema/UX change, but a future iteration should validate usernames against `^[A-Za-z0-9_]{3,30}$` so the encode-decode round-trip becomes a non-issue
lesson: Next.js client-side useParams returns the *undecoded* URL segment — wrap with `decodeURIComponent` (try/catch for malformed) before using as a DB key
status: shipped (pending push)

### grubgauge — public profile route `/u/[username]` — 2026-05-12
worked: extracted the Explore inline card into `<RatingCard>` with `rank?` + `hideRater?` props (Explore behaves identically — same key-on-hydration trick, same indent, same render order); added `getProfileByUsername` to `lib/profile/profile.ts` (maybeSingle, returns null on miss); built the route as a client page mirroring Explore's fetch shape (useEffect + Promise.all for likes); used `notFound()` from render guarded behind `!loading` so the 404 only fires once after resolution; skipped `attachRaters` on this page (rater is always the page subject, badge is hidden, so the field is `null` — one round-trip saved); header total computed from `countMap` with an inline comment documenting the post-load drift trade-off (intentional, not bug)
didn't: didn't introduce a custom `not-found.tsx` — the framework's `/_not-found` is fine until there's something profile-specific to show there; didn't refactor History/Dashboard to use `<RatingCard>` (their card shapes diverge — History has an edit affordance, Dashboard has a "Top Rated" callout variant) — left a top-of-file note on Explore so a future convergence pass has the spec
lesson: when extracting a list-item component from one of N near-duplicate inline blocks, ship only the call site you need today plus an in-file note pointing future convergence at the divergent shapes — premature unification across all N sites locks in the wrong abstraction when one of them later needs different chrome
status: shipped (typecheck + build clean; lint shows only the same two pre-existing setLoading-in-effect errors in pages I didn't touch)

### grubgauge — rater attribution on Explore rating card — 2026-05-12
worked: shipped two small reusable pieces — `getRatersByUserIds` / `attachRaters` in `lib/profile/raters.ts` (batched, dedup, Map-keyed) and `<RaterBadge>` that mirrors HomeHeader.ProfileAvatar chrome (h-8 w-8, label-sm fallback) so the header avatar and card-level attribution feel like a matched set; Explore fetches raters in the same Promise.all as likes (3 parallel round-trips total, still O(1) per card render); deleted-user / orphan-rating branches converge on the same `rater === null` rendering (fallback avatar + italic "Deleted user", no Link)
didn't: skipped a PostgREST embedded select because `ratings.user_id` FK targets `auth.users(id)` not `profiles(id)` — PostgREST chains an embed through a direct FK only, and the codebase already prefers flat selects + batched hydrators; added `user_id` to History + Dashboard selects for type consistency even though those surfaces don't render attribution (rater is always the current user there)
lesson: PostgREST embedded selects need a direct FK between the joined tables; chains through a shared third table (here, auth.users) don't resolve — either add a parallel FK or use a batched two-query hydrator (cheaper to maintain in a flat-select codebase)
status: shipped (typecheck + build clean; lint shows only the two pre-existing setLoading-in-effect errors in pages I didn't modify in this iteration)

### grubgauge — public.profiles cutover (migrate username/avatar_url out of user_metadata) — 2026-05-12
worked: single migration handles table + RLS + signup trigger + idempotent backfill (with NOTICE-logged collision suffixes) + targeted strip of the three migrated keys; app side gets a useProfile() hook + upsertProfile() helper with a `profile:updated` window event so the header avatar stays live across writes without a navigation; tsc + build green, lint unchanged from prior baseline
didn't: the lint rule for set-state-in-effect fires unpredictably (line 36 was unflagged in one run then flagged after removing a sibling disable directive) — wasted one cycle figuring out the dependency between disables on different blocks
lesson: ESLint disable directives can suppress reports that share inferred control flow, so removing one disable can surface another — make changes in isolation and re-run rather than removing in bulk
status: shipped (pending paste of migration into Supabase SQL Editor; check NOTICE output for collision list and verification counts)

### grubgauge — like button on rating card (Explore feed) — 2026-05-12
worked: extracted reusable <LikeButton> on Explore (the community feed); batched both liked-set and counts via a new getRatingsLikeCounts companion in likes.ts (parallel to getUserLikedRatings) so each card is hydrated in 2 round-trips total instead of 2N; tsc + build clean
didn't: no extracted rating-card component existed (three feeds render the card JSX inline) — chose Explore because liking your own History rating is bad UX; documented the duplication in a top-of-file comment so a future extraction has the spec
lesson: when a feed needs per-card auth-aware state, ship the batched hydrator alongside the per-item action — N+1 round-trips is the failure mode you can't see in dev with a 3-row test set
status: shipped

### grubgauge — likes server-side functions — 2026-05-12
worked: pre-flight checked for server actions + API routes (neither used) and matched the existing browser-client helper shape from avatar.ts / scope.ts; result-type return for toggleLike keeps the unauth path out of try/catch
didn't: nothing of note — single new file, no edits to existing code, tsc + lint green on first pass
lesson: in browser-client-mutation codebases, use a discriminated-union result type (not thrown errors) for any auth-gated mutation the UI must route around
status: shipped

### grubgauge — rating_likes migration — 2026-05-12
worked: pre-flight confirmed the ratings PK before writing the FK, file matched existing migration shape (timestamp slot, idempotent guards, drop-then-create policy pattern)
didn't: spec said "delete with check user_id = auth.uid()" — Postgres RLS uses USING for DELETE, not WITH CHECK; flagged the translation in the SQL comment
lesson: Postgres RLS clause by command — SELECT/DELETE use USING; INSERT uses WITH CHECK; UPDATE uses both
status: shipped (pending paste into Supabase SQL Editor)

### grubgauge — profile photo upload — 2026-05-12
worked: one storage migration + one reusable AvatarUploader landed cleanly across onboarding, profile, and header; build + tsc + lint on new files all green
didn't: spent a lint cycle on a `useEffect(() => setState(prop))` prop-mirror pattern that R19's new `react-hooks/set-state-in-effect` rule rejects — should have written the override-in-render shape on the first pass
lesson: for "seeded by prop, locally settable" UI state, derive in render via `override: T | null` + `display = override ?? prop`; never `useEffect(() => setState(prop), [prop])`
status: shipped (pending Supabase migration run + git push)
